package partials

templ Filters() {
    <div class="flex flex-col gap-2">
        <div class="grid gap-2">
            @FilterButton("Grayscale", "/api/grayscale")
            @FilterButton("Binary", "/api/binary")
        </div>
    </div>
	<script>

    </script>
}
